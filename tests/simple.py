def myFunc(x, y, z):
    vec = [x, True, False, 0]
    print(vec)
    return sum(x, y, z)


x = "123"

def f(x, y):
    if x == y:
        return "they are equal"
    elif x > y:
        return "x is bigger"
    else:
        return "x is smaller"
    

# def has diff struct.

inc = lambda x: sum(x,1)

